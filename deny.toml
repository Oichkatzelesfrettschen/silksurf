# Workspace-wide cargo-deny policy for SilkSurf

[graph]
targets = [
  "x86_64-unknown-linux-gnu",
  "x86_64-apple-darwin",
  "aarch64-apple-darwin",
  "x86_64-pc-windows-msvc",
  "wasm32-unknown-unknown",
]
all-features = false
exclude-dev = false

[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
ignore = [
  # rustls-pemfile is unmaintained (RUSTSEC-2025-0134). Used by silksurf-tls
  # for the --tls-ca-file CLI loader. The migration to rustls-pki-types::pem::
  # PemObject is a real refactor (one file, but API shape changes); tracked
  # as a follow-up task in the SNAZZY-WAFFLE roadmap. Re-evaluate the ignore
  # at every roadmap-wave boundary.
  { id = "RUSTSEC-2025-0134", reason = "tracked migration to rustls-pki-types::pem::PemObject; not vulnerable, just unmaintained" },
]

[licenses]
confidence-threshold = 0.93
allow = [
  "MIT",
  "Apache-2.0",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "ISC",
  "Zlib",
  "CDLA-Permissive-2.0",
  "Unicode-3.0",
  "CC0-1.0",
]
exceptions = []

[licenses.private]
ignore = true
registries = []

[[licenses.clarify]]
crate = "ring"
expression = "ISC AND MIT AND OpenSSL"
license-files = [{ path = "LICENSE", hash = 0xbd0eed23 }]

[[licenses.clarify]]
crate = "webpki"
expression = "ISC"
license-files = [{ path = "LICENSE", hash = 0x001c7e6c }]

[bans]
multiple-versions = "warn"
wildcards = "allow"
workspace-default-features = "allow"
external-default-features = "allow"
highlight = "all"
deny = [
  { crate = "openssl", use-instead = "rustls" },
  { crate = "openssl-sys", use-instead = "rustls" },
  { crate = "git2", use-instead = "gix" },
  { crate = "libgit2-sys", use-instead = "gix" },
  { crate = "atty", use-instead = "std::io::IsTerminal" },
  { crate = "ansi_term", use-instead = "owo-colors or yansi" },
]
skip = []
skip-tree = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []

[output]
feature-depth = 1
