# Legacy C Headers

Headers under `include/` are part of the legacy C baseline. Keep them as
cleanroom reference only; Rust sources live under `crates/`.

See `docs/LEGACY_C_PORTING.md` for the migration inventory.
